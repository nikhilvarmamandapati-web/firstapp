import 'package:flutter/material.dart';

void main() {
  runApp(
    MaterialApp(
      debugShowCheckedModeBanner: false,
      home: Scaffold(
        backgroundColor: Colors.lightBlue.shade100,
        appBar: AppBar(
          title: const Text("Three Icons"),
          centerTitle: true,
        ),
        body: Center(
          child: Column(
            mainAxisAlignment: MainAxisAlignment.center,
            children: [
              // Icons above the red container
              const Row(
                mainAxisAlignment: MainAxisAlignment.center,
                children: [
                  Icon(
                    Icons.home,
                    size: 40,
                    color: Colors.black,
                  ),
                  SizedBox(width: 40),
                  Icon(
                    Icons.favorite,
                    size: 40,
                    color: Colors.red,
                  ),
                  SizedBox(width: 40),
                  Icon(
                    Icons.settings,
                    size: 40,
                    color: Colors.blue,
                  ),
                ],
              ),

              const SizedBox(height: 20),

              // Red container
              Container(
                width: 400,
                height: 300,
                color: Colors.redAccent,
                alignment: Alignment.center,
                child: Container(
                  width: 150,
                  height: 140,
                  color: Colors.green,
                ),
              ),
            ],
          ),
        ),
      ),
    ),
  );
}
